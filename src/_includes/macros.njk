{% macro render_post_list(tag) %}
  {% for post in collections[tag] | reverse %}
    <div class="mb-2">
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      {# <span class="text-gray-500 ml-2">({% formatDate post.date %})</span> #}
    </div>
  {% endfor %}
{% endmacro %}