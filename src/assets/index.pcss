@tailwind base;

@tailwind components;

@tailwind utilities;

html {
  /* @apply bg-gray-50; */
  font-family: 'Noto Serif SC', sans-serif;
  @apply text-gray-900;
}

.logo {
  font-family: 'Josefin Sans', sans-serif;
}

p {
  @apply mb-2;
}